import type { INodeProperties } from "n8n-workflow";

/**
 * Property definitions for 'qsv describegpt' operation.
 * Auto-generated by scripts/generate-qsv-nodes.ts
 */
export const DescribegptDescription: INodeProperties[] = [
  {
    displayName: "Input CSV File Path",
    name: "inputPath",
    type: "string",
    default: "",
    required: true,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "The absolute or relative file path to the input CSV file",
  },
  {
    displayName: "Output File Path",
    name: "outputPath",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Path where the output will be saved. If empty, result may be returned in stdout/JSON.",
  },
  {
    displayName: "Dictionary",
    name: "dictionary",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Create a Data Dictionary using a hybrid "neuro-symbolic" pipeline - i.e. the Dictionary is populated deterministically using Summary Statistics and Frequency Distribution data, and only the human-friendly Label and Description (and Content Type when --infer-content-type is set) are populated by the LLM using the same statistical context. The dataset\'s content language is also auto-detected locally (using whatlang, not the LLM) from sampled values of its text columns, and emitted as detected_language, detected_language_code (ISO 639-3) and detected_language_confidence in the JSON dictionary (top-level) and JSON Schema ("x-qsv" object) outputs. A language is only recorded when the full sample AND each of 3 disjoint sub-samples independently agree on it - text genuinely written in one language is stable across its own sub-samples, while a table of proper nouns (city, country and person names) is not. The confidence is reported but does not decide, as it measures the margin between the top two candidate languages rather than whether the text is in either of them. The fields are omitted when the sub-samples disagree, and also when an explicit language is given with the --language option. When a language IS recorded, the LLM is also asked to generate its output (Labels, Descriptions, Tags and the dataset Description) in it. The Dictionary is a DRAFT for a human to review. The deterministic half is reproducible, but the LLM half is not: inferring twice over the same data can return different Labels, and (under --infer-content-type) different roles and concepts. So save it, review it, correct it, and keep the corrected copy - that file, not the model, is what makes a later run reproducible. "qsv viz smart --dictionary infer" builds this same dictionary and does exactly that, saving it beside the input as <stem>.schema.json and reusing it on every later run.',
  },
  {
    displayName: "Description",
    name: "description",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Infer a general Description of the dataset based on detailed statistical context. An Attribution signature is embedded in the Description.",
  },
  {
    displayName: "Tags",
    name: "tags",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Infer Tags that categorize the dataset based on detailed statistical context. Useful for grouping datasets and filtering.",
  },
  {
    displayName: "All",
    name: "all",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "Shortcut for --dictionary --description --tags.",
  },
  {
    displayName: "Num Examples",
    name: "numExamples",
    type: "string",
    default: "5",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The number of Example values to include in the dictionary. [default: 5]",
  },
  {
    displayName: "Truncate Str",
    name: "truncateStr",
    type: "string",
    default: "25",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The maximum length of an Example value in the dictionary. An ellipsis is appended to the truncated value. If zero, no truncation is performed. Bounds the prompt sent to the LLM, and applies to the csv/json/semantic-md dictionary output. The jsonschema output is machine-facing, so it always carries the full, untruncated Example values. [default: 25]",
  },
  {
    displayName: "Infer Content Type",
    name: "inferContentType",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Also have the LLM classify each field\'s semantic "Content Type", mapped to a curated, documented vocabulary (e.g. email, city, category, name, credit card, etc.) see https://github.com/dathere/qsv/blob/master/src/cmd/synthesize/faker_map.rs. Adds a "Content Type" column/field to the Data Dictionary output. Fields where cardinality equals the row count (i.e. every row has a distinct non-null value - primary keys, surrogate keys, sequence numbers) are deterministically classified as "unique_id", overriding any token the LLM returned for that field. For Date/DateTime fields, the LLM also infers the column\'s strftime date format (e.g. "date:%m/%d/%Y"); the Markdown, JSON & JSON Schema dictionaries then render Min/Max AND Examples in that inferred format so they match how the dates actually appear in the data, instead of qsv\'s normalized form. (TSV output keeps Min/Max & Examples in qsv\'s raw normalized form.) For a CONTINUOUS numeric measure on a canonical scale (a percentage, a 0-1 ratio/probability, a bounded index), the LLM also proposes an "x-qsv.gauge_range" [min, max]; qsv keeps it only when the field is a numeric measure AND the observed data lies within it, so that a "viz smart" dictionary-driven Data Schematic draws that KPI tile as a GAUGE. (A KPI "vs target" delta uses "x-qsv.target", which is a GOAL you hand-author - never inferred.) For a MONETARY measure the LLM also proposes an "x-qsv.currency": the ISO-4217 alpha-3 code (e.g. "USD") the amounts are denominated in. Codes are validated against the ISO register, and qsv keeps one only when the field is a numeric measure that reads as money, so a currency on a count or on the column that NAMES the currency (that column is a "currency_code" dimension) is dropped. Money columns are also tagged with the "money" content type and the "measure.money" concept. "qsv viz smart --dictionary" reads the code to prefix that column\'s KPI tile with the currency\'s symbol. The LLM also infers dataset-level INTER-COLUMN RELATIONSHIPS, emitted as a "relationships" array (top-level in the JSON dictionary, and in the dataset-level "x-qsv" object of the JSON Schema one). Each entry has a "kind" of "joint", "ordered", "correlated" or "pipeline", plus a "members" list of the field names involved. A "pipeline" is a process whose stages narrow monotonically - planned to committed to spent, impressions to clicks to conversions. Its stages may be separate COLUMNS (listed in "members", upstream first - the opposite direction from "ordered", which ascends) or the VALUES of one category column (via "stage_column", an ordered "stages" list, and an optional "value_column" to sum). "qsv viz smart --dictionary" reads these to draw its pipeline funnel panel; edit them in the saved schema to correct or add one.',
  },
  {
    displayName: "Infer Null Values",
    name: "inferNullValues",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Also have the LLM propose each field\'s null sentinels - literal values that stand in for "missing" (e.g. NULL, N/A, -999, 9999-12-31). Emitted into the JSON Schema dictionary\'s per-property "x-qsv" object, split into two lists that carry different warranties. * "null_values" - proposed by the LLM AND independently confirmed by qsv to occur as a literal value in that String column. Every observed casing is listed. * "null_candidates" - proposed by the LLM but NOT confirmable by any scan; numeric/date placeholders that parse as valid values of the column\'s own type (-999 is a legal integer), or tokens never seen in the data. Each carries "confirm_required": true. A proposal that is IMPOSSIBLE is dropped rather than reported: a non-numeric token in a numeric column (no cell could hold it, or the column would not have typed as numeric), and an echo of the null placeholder the Frequency Distribution displays for cells qsv already counts as empty. A confirmed "null_values" entry means only that the literal is PRESENT in the column; the judgment that it MEANS "missing" remains the LLM\'s. This complements "qsv denull", which reports a narrower set - only the columns that would promote to a numeric type once their sentinels are blanked. A purely categorical column (status = ok/pending/NULL) is outside denull\'s remit. Sentinels are REPORTED, never applied - qsv does not modify your data on an LLM\'s say-so. Only "qsv denull --apply" masks values, and only in the columns it independently confirmed. Only rendered by "--format JSONSchema"; the other formats ignore them.',
  },
  {
    displayName: "Two Pass",
    name: "twoPass",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Run a second LLM call that takes the full first-pass Data Dictionary as JSON context and refines each field's Label, Description and (when --infer-content-type is set) Content Type using cross-field awareness. The LLM can then relate fields that belong together (e.g. street_no + street_name + city + state + zip describing a single mailing address; first_name + last_name naming a single person; lat + lng forming a coordinate pair). The refined dictionary becomes the emitted output and is also what downstream Description, Tags and Prompt inference phases see as dictionary context. Roughly doubles dictionary LLM cost and latency, so opt-in. Most useful when combined with --infer-content-type. Allowed with the --dictionary, --all and --prompt inference flags. Mutually exclusive with --prepare-context and --process-response (MCP sampling is single-turn per inference phase).",
  },
  {
    displayName: "Addl Cols",
    name: "addlCols",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Add additional columns to the dictionary from the Summary Statistics.",
  },
  {
    displayName: "Addl Cols List",
    name: "addlColsList",
    type: "string",
    default: "sort_order, sortiness, mean, median, mad, stddev, variance, cv",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'A comma-separated list of additional stats columns to add to the dictionary. The columns must be present in the Summary Statistics. If the columns are not present in the Summary Statistics or already in the dictionary, they will be ignored. These values are case-insensitive and automatically set the --addl-cols option to true. "everything" can be used to add all 45 "available" statistics columns. You can adjust the available columns with --stats-options. "everything!" automatically sets --stats-options to compute "all" 51 supported stats. The 6 addl cols are the mode/s & antimode/s stats with each having counts & occurrences. "moar" gets you even moar stats, with detailed outliers info. "moar!" gets you even moar with --advanced stats (Kurtosis, Gini Coefficient & Shannon Entropy) [default: sort_order, sortiness, mean, median, mad, stddev, variance, cv]',
  },
  {
    displayName: "Num Tags",
    name: "numTags",
    type: "string",
    default: "10",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The maximum number of tags to infer when the --tags option is used. Maximum allowed value is 50. [default: 10]",
  },
  {
    displayName: "Tag Vocab",
    name: "tagVocab",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The CSV file containing the tag vocabulary to use for inferring tags. If no tag vocabulary file is provided, the model will use free-form tags. Supports local files, remote URLs (http/https), CKAN resources (ckan://), and dathere:// scheme. Remote resources are cached locally. The CSV file must have two columns with headers: first column is the tag, second column is the description. Note that qsvlite only supports local files.",
  },
  {
    displayName: "Cache Dir",
    name: "cacheDir",
    type: "string",
    default: "~/.qsv-cache",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The directory to use for caching downloaded tag vocabulary resources. If the directory does not exist, qsv will attempt to create it. If the QSV_CACHE_DIR envvar is set, it will be used instead. [default: ~/.qsv-cache]",
  },
  {
    displayName: "Ckan Api",
    name: "ckanApi",
    type: "string",
    default: "https://data.dathere.com/api/3/action",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'The URL of the CKAN API to use for downloading tag vocabulary resources with the "ckan://" scheme. If the QSV_CKAN_API envvar is set, it will be used instead. [default: https://data.dathere.com/api/3/action]',
  },
  {
    displayName: "Ckan Token",
    name: "ckanToken",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The CKAN API token to use. Only required if downloading private resources. If the QSV_CKAN_TOKEN envvar is set, it will be used instead.",
  },
  {
    displayName: "Stats Options",
    name: "statsOptions",
    type: "string",
    default:
      "--infer-dates --infer-boolean --mad --quartiles --percentiles --force --stats-jsonl",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Options for the stats command used to generate summary statistics. If it starts with "file:" prefix, the statistics are read from the specified CSV file instead of running the stats command. e.g. "file:my_custom_stats.csv" [default: --infer-dates --infer-boolean --mad --quartiles --percentiles --force --stats-jsonl]',
  },
  {
    displayName: "Freq Options",
    name: "freqOptions",
    type: "string",
    default: "--rank-strategy dense",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Options for the frequency command used to generate frequency distributions. You can use this to exclude certain variable types from frequency analysis (e.g., --select \'!id,!uuid\'), limit results differently per use case, or control output format. If --limit is specified here, it takes precedence over --enum-threshold. If it starts with "file:" prefix, the frequency data is read from the specified CSV file instead of running the frequency command. e.g. "file:my_custom_frequency.csv" A "file:"-backed CSV is assumed to use frequency\'s default "(NULL)" null text; a custom --null-text in a file-supplied CSV is not recognized when validating inferred date/datetime formats. [default: --rank-strategy dense]',
  },
  {
    displayName: "Enum Threshold",
    name: "enumThreshold",
    type: "string",
    default: "10",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'The threshold for compiling Enumerations with the frequency command before bucketing other unique values into the "Other" category. This is a convenience shortcut for --freq-options --limit <n>. If --freq-options contains --limit, this flag is ignored. [default: 10]',
  },
  {
    displayName: "Prompt",
    name: "prompt",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Custom prompt to answer questions about the dataset. The prompt will be answered based on the dataset\'s Summary Statistics, Frequency data & Data Dictionary. If the prompt CANNOT be answered by looking at these metadata, a SQL query will be generated to answer the question. If the "polars" or the "QSV_DUCKDB_PATH" environment variable is set & the `--sql-results` option is used, the SQL query will be automatically executed and its results returned. Otherwise, the SQL query will be returned along with the reasoning behind it. If it starts with "file:" prefix, the prompt is read from the file specified. e.g. "file:my_long_prompt.txt"',
  },
  {
    displayName: "Sql Results",
    name: "sqlResults",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'The file to save the SQL query results to. Only valid if the --prompt option is used & the "polars" or the "QSV_DUCKDB_PATH" environment variable is set. If the SQL query executes successfully, the results will be saved with a ".csv" extension. Otherwise, it will be saved with a ".sql" extension so the user can inspect why it failed and modify it.',
  },
  {
    displayName: "Prompt File",
    name: "promptFile",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The configurable TOML file containing prompts to use for inferencing. If no file is provided, default prompts will be used. The prompt file uses the MiniJinja template engine (https://docs.rs/minijinja) See https://github.com/dathere/qsv/blob/master/resources/describegpt_defaults.toml",
  },
  {
    displayName: "Context File",
    name: "contextFile",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Path to a file with additional context about the dataset - e.g. variable/code labels, provenance & domain notes - injected into the prompts as the {{ context }} MiniJinja variable. The file TYPE is",
  },
  {
    displayName: "Markdown Template",
    name: "markdownTemplate",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "TOML file with MiniJinja templates for Markdown output. The TOML",
  },
  {
    displayName: "Sample Size",
    name: "sampleSize",
    type: "string",
    default: "100",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The number of rows to randomly sample from the input file for the sample data. Uses the INDEXED sampling method with the qsv sample command. [default: 100]",
  },
  {
    displayName: "Fewshot Examples",
    name: "fewshotExamples",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "By default, few-shot examples are NOT included in the LLM prompt when generating SQL queries. When this option is set, few-shot examples in the default prompt file are included. Though this will increase the quality of the generated SQL, it comes at a cost - increased LLM API call cost in terms of tokens and execution time. See https://en.wikipedia.org/wiki/Prompt_engineering for more info.",
  },
  {
    displayName: "Session",
    name: "session",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Enable stateful session mode for iterative SQL RAG refinement. The session name is the file path of the markdown file where session messages will be stored. When used with --prompt, subsequent queries in the same session will refine the baseline SQL query. SQL query results (10-row sample) and errors are automatically included in subsequent messages for context.",
  },
  {
    displayName: "Session Len",
    name: "sessionLen",
    type: "string",
    default: "10",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Maximum number of recent messages to keep in session context before summarizing older messages. Only used when --session is specified. [default: 10]",
  },
  {
    displayName: "No Score Sql",
    name: "noScoreSql",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Disable scoresql validation of generated SQL queries before execution. By default, when --prompt generates a SQL query and --sql-results is set, the query is scored and iteratively improved if below threshold.",
  },
  {
    displayName: "Score Threshold",
    name: "scoreThreshold",
    type: "string",
    default: "50",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Minimum scoresql score for a SQL query to be accepted. Typical range is 0-100; values >100 will always trigger retries and the below-threshold warning. [default: 50]",
  },
  {
    displayName: "Score Max Retries",
    name: "scoreMaxRetries",
    type: "string",
    default: "3",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Max LLM re-prompts to improve a low-scoring SQL query. [default: 3]",
  },
  {
    displayName: "Base Url",
    name: "baseUrl",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "The LLM API URL. Supports APIs & local LLMs compatible with",
  },
  {
    displayName: "Model",
    name: "model",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The model to use for inferencing. This model must be compatible with OpenAI API spec. Works with both cloud LLM providers and local LLMs. Tested open weights models include OpenAI's gpt-oss-20b and gpt-oss-120b; Google's Gemma family of open models; and Mistral's Magistral reasoning models. Precedence: explicit CLI flag > QSV_LLM_MODEL env var > prompt file model > built-in default (google/gemma-4-26b-a4b). No docopt default — same rationale as --base-url above.",
  },
  {
    displayName: "Language",
    name: "language",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'The output language/dialect/tone to use for the response. (e.g., "Spanish", "French", "Hindi", "Mandarin", "Italian", "Castilian", "Franglais", "Taglish", "Pig Latin", "Valley Girl", "Pirate", "Shakespearean English", "Chavacano", "Gen Z", "Yoda", etc.) If set to a string, that language/dialect is ALWAYS used for the response, overriding auto-detection AND suppressing the recorded detection entirely, so the detected_language fields are omitted. If set to a float (0.0 to 1.0), it specifies a minimum confidence floor. If not set, inference mode applies no confidence floor (see below) and chat mode uses 80%.',
  },
  {
    displayName: "Addl Props",
    name: "addlProps",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Additional model properties to pass to the LLM chat/completion API. Various models support different properties beyond the standard ones. For instance, gpt-oss-20b supports the "reasoning_effort" property. e.g. to set the "reasoning_effort" property to "high" & "temperature" to 0.5, use \'{"reasoning_effort": "high", "temperature": 0.5}\'',
  },
  {
    displayName: "Api Key",
    name: "apiKey",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The API key to use. If set, takes precedence over the QSV_LLM_APIKEY envvar. Required when the base URL is not localhost. Set to NONE to suppress sending the API key.",
  },
  {
    displayName: "Max Tokens",
    name: "maxTokens",
    type: "string",
    default: "10000",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Limits the number of generated tokens in the output. Set to 0 to disable token limits. If the --base-url is localhost, indicating a local LLM, the default is automatically set to 0. [default: 10000]",
  },
  {
    displayName: "Timeout",
    name: "timeout",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Timeout for completions in seconds. If 0, no timeout is used. Defaults to 300 if not set. If the --base-url is localhost, indicating a local LLM, the timeout is automatically disabled unless you set --timeout (or the QSV_TIMEOUT envvar) explicitly.",
  },
  {
    displayName: "User Agent",
    name: "userAgent",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Specify custom user agent. It supports the following variables - $QSV_VERSION, $QSV_TARGET, $QSV_BIN_NAME, $QSV_KIND and $QSV_COMMAND. Try to follow the syntax here - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/User-Agent",
  },
  {
    displayName: "Export Prompt",
    name: "exportPrompt",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Export the default prompts to the specified file that can be used with the --prompt-file option. The file will be saved with a .toml extension. If the file already exists, it will be overwritten. It will exit after exporting the prompts.",
  },
  {
    displayName: "No Cache",
    name: "noCache",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "Disable default disk cache.",
  },
  {
    displayName: "Disk Cache Dir",
    name: "diskCacheDir",
    type: "string",
    default: "~/.qsv-cache/describegpt",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "The directory to store the disk cache. Note that if the directory does not exist, it will be created. If the directory exists, it will be used as is, and will not be flushed. This option allows you to maintain several disk caches for different describegpt jobs (e.g. one for a data portal, another for internal data exchange). [default: ~/.qsv-cache/describegpt]",
  },
  {
    displayName: "Redis Cache",
    name: "redisCache",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'Use Redis instead of the default disk cache to cache LLM completions. It connects to "redis://127.0.0.1:6379/3" by default, with a connection pool size of 20, with a TTL of 28 days, and cache hits NOT refreshing an existing cached value\'s TTL. This option automatically disables the disk cache.',
  },
  {
    displayName: "Fresh",
    name: "fresh",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Send a fresh request to the LLM API, refreshing a cached response if it exists. When a --prompt SQL query fails, you can also use this option to request the LLM to generate a new SQL query.",
  },
  {
    displayName: "Forget",
    name: "forget",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "Remove a cached response if it exists and then exit.",
  },
  {
    displayName: "Flush Cache",
    name: "flushCache",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Flush the current cache entries on startup. WARNING: This operation is irreversible.",
  },
  {
    displayName: "Prepare Context",
    name: "prepareContext",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Output the prompt context as JSON to stdout without calling the LLM. JSON includes system/user prompts, cache state, and analysis results for each inference phase. Useful for inspecting prompts or piping to custom LLM integrations. Used by the MCP server for sampling mode.",
  },
  {
    displayName: "Process Response",
    name: "processResponse",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Process LLM responses provided as JSON via stdin. Takes the output format from --prepare-context with LLM responses filled in, and produces the final output (dictionary, description, tags, or prompt results). Used by the MCP server for sampling mode.",
  },
  {
    displayName: "Format",
    name: "format",
    type: "string",
    default: "Markdown",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "Output format: Markdown, TSV, JSON, TOON, JSONSchema, SemanticMd, or OKF. TOON is a compact, human-readable encoding of the JSON data model for LLM prompts. See https://toonformat.dev/ for more info. JSONSchema emits the Data Dictionary as a JSON Schema (draft 2020-12) document, enriched with LLM-inferred Label, Description and Content Type (the latter only when the infer-content-type flag is set). qsv- and LLM- specific metadata not modeled by the JSON Schema spec (cardinality, null_count, weighted example counts, content_type, null_values, null_candidates, addl stats columns) is preserved via a single x-qsv annotation object per property; unknown keywords are ignored by validators per the 2020-12 spec. The JSONSchema format requires the dictionary inference phase (the dictionary or all flag). The description inference, when also run, becomes the schema's top-level description; tags, when also run, are embedded at x-qsv.tags. The prompt inference is not supported. SemanticMd emits the Data Dictionary as a Semantic Markdown document (https://semanticmd.org/) - human-readable markdown with light, agent-parseable conventions that a companion converter turns into JSON. It enriches each column with a catalog-wide Concept ID (for cross-dataset join discovery), an analytical Role (dimension/measure/identifier/timestamp), join keys & cardinality, data-quality flags, and a richer per-column statistics block; it also emits a dataset grain and a temporal/spatial envelope. To populate Concept/Role/grain, SemanticMd implies --infer-content-type. Like JSONSchema, it requires the dictionary inference phase (the dictionary or all flag). The description inference, when also run, becomes the '# Dataset' description; tags, when also run, are embedded in the document frontmatter. The prompt inference is not supported. OKF emits the Data Dictionary as an Open Knowledge Format document (https://github.com/GoogleCloudPlatform/knowledge-catalog/tree/main/okf) - a leaner, vendor-neutral plain-markdown-plus-YAML-frontmatter format. It emits frontmatter (type/title/description/resource/timestamp/tags) and a Schema section with a Column/Type/Content Type/Description/Enumeration table. The type frontmatter key is set via the okf-type flag (default \"CSV Table\"); ds-source maps to resource and ds-updated maps to timestamp. Like SemanticMd, OKF implies infer-content-type, requires the dictionary inference phase (the dictionary or all flag), folds the description inference into the body prose and a single-sentence frontmatter description, and embeds tags in the frontmatter. The prompt inference is not supported. [default: Markdown]",
  },
  {
    displayName: "Allow Extra Cols",
    name: "allowExtraCols",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "When the format is JSONSchema, emit additionalProperties as true at the schema root (default is false, strict). Only meaningful with the JSONSchema format; ignored otherwise.",
  },
  {
    displayName: "Strict Dates",
    name: "strictDates",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "When the format is JSONSchema, emit format date or date-time for columns that stats infers as Date or DateTime. Off by default because qsv's --infer-dates is permissive (accepts strings like \"June 27, 1968\") and JSON Schema's date formats require RFC 3339, so the validate roundtrip would otherwise fail. Set this only when your source columns are guaranteed to be RFC 3339 full-date / date-time. Mirrors the same flag on the schema command.",
  },
  {
    displayName: "Ds Source",
    name: "dsSource",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "For the SemanticMd & OKF formats only: the dataset source/provenance recorded in the document frontmatter (e.g. a source URL or publisher). For OKF this populates the `resource` key. Optional; the frontmatter key is omitted when unset. Ignored by other formats.",
  },
  {
    displayName: "Ds Updated",
    name: "dsUpdated",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "For the SemanticMd & OKF formats only: the dataset's last-updated date recorded in the document frontmatter. For OKF this populates the `timestamp` key. Optional. Ignored by other formats.",
  },
  {
    displayName: "Ds License",
    name: "dsLicense",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      "For the SemanticMd format only: the dataset license recorded in the document frontmatter. Optional. Ignored by other formats.",
  },
  {
    displayName: "Okf Type",
    name: "okfType",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description:
      'For the OKF format only: the value of the required `type` frontmatter key (e.g. "CSV Table", "BigQuery Table"). Optional; defaults to "CSV Table". Ignored by other formats.',
  },
  {
    displayName: "Quiet",
    name: "quiet",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["describegpt"],
      },
    },
    description: "Do not print status messages to stderr.",
  },
];
