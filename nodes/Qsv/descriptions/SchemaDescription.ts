import type { INodeProperties } from "n8n-workflow";

/**
 * Property definitions for 'qsv schema' operation.
 * Auto-generated by scripts/generate-qsv-nodes.ts
 */
export const SchemaDescription: INodeProperties[] = [
  {
    displayName: "Input CSV File Path",
    name: "inputPath",
    type: "string",
    default: "",
    required: true,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description: "The absolute or relative file path to the input CSV file",
  },
  {
    displayName: "Output File Path",
    name: "outputPath",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Path where the output will be saved. If empty, result may be returned in stdout/JSON.",
  },
  {
    displayName: "Enum Threshold",
    name: "enumThreshold",
    type: "string",
    default: "50",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Cardinality threshold for adding enum constraints. Enum constraints are compiled for String & Integer types. [default: 50]",
  },
  {
    displayName: "Ignore Case",
    name: "ignoreCase",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Ignore case when compiling unique values for enum constraints. Do note however that the `validate` command is case-sensitive when validating against enum constraints.",
  },
  {
    displayName: "Strict Dates",
    name: "strictDates",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      'Enforce Internet Datetime format (RFC-3339) for detected date/datetime columns. Otherwise, even if columns are inferred as date/datetime, they are set to type "string" in the schema instead of "date" or "date-time".',
  },
  {
    displayName: "Strict Formats",
    name: "strictFormats",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Enforce JSON Schema format constraints for detected email, hostname, and IP address columns. When enabled, String fields are checked against email, hostname, IPv4, and IPv6 formats. Format constraints are only added if ALL unique values in the field match the detected format.",
  },
  {
    displayName: "Pattern Columns",
    name: "patternColumns",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Select columns to derive regex pattern constraints. That is, this will create a regular expression that matches all values for each specified column. Columns are selected using `select` syntax (see `qsv select --help` for details).",
  },
  {
    displayName: "Dates Whitelist",
    name: "datesWhitelist",
    type: "string",
    default: "date,time,due,open,close,created",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      'The case-insensitive patterns to look for when shortlisting fields for date inference. i.e. if the field\'s name has any of these patterns, it is shortlisted for date inferencing. Set to "all" to inspect ALL fields for date/datetime types. [default: date,time,due,open,close,created]',
  },
  {
    displayName: "Prefer Dmy",
    name: "preferDmy",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Prefer to parse dates in dmy format. Otherwise, use mdy format.",
  },
  {
    displayName: "Force",
    name: "force",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Force recomputing cardinality and unique values even if stats cache file exists and is current.",
  },
  {
    displayName: "Stdout",
    name: "stdout",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description: "Send generated JSON schema file to stdout instead.",
  },
  {
    displayName: "Jobs",
    name: "jobs",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "The number of jobs to run in parallel. When not set, the number of jobs is set to the number of CPUs detected.",
  },
  {
    displayName: "Polars",
    name: "polars",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Infer a Polars schema instead of a JSON Schema. This option is only available if the `polars` feature is enabled. The generated Polars schema will be written to a file with the `.pschema.json` suffix appended to the input filename.",
  },
  {
    displayName: "No Headers",
    name: "noHeaders",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "When set, the first row will not be interpreted as headers. Namely, it will be processed with the rest of the rows. Otherwise, the first row will always appear as the header row in the output.",
  },
  {
    displayName: "Delimiter",
    name: "delimiter",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "The field delimiter for reading CSV data. Must be a single character.",
  },
  {
    displayName: "Memcheck",
    name: "memcheck",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["schema"],
      },
    },
    description:
      "Check if there is enough memory to load the entire CSV into memory using CONSERVATIVE heuristics.",
  },
];
