import type { INodeProperties } from "n8n-workflow";

/**
 * Property definitions for 'qsv apply' operation.
 * Auto-generated by scripts/generate-qsv-nodes.ts
 */
export const ApplyDescription: INodeProperties[] = [
  {
    displayName: "Input CSV File Path",
    name: "inputPath",
    type: "string",
    default: "",
    required: true,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "The absolute or relative file path to the input CSV file",
  },
  {
    displayName: "Output File Path",
    name: "outputPath",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Path where the output will be saved. If empty, result may be returned in stdout/JSON.",
  },
  {
    displayName: "New Column",
    name: "newColumn",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "Put the transformed values in a new column instead.",
  },
  {
    displayName: "Rename",
    name: "rename",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "New name for the transformed column.",
  },
  {
    displayName: "Jobs",
    name: "jobs",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "The number of jobs to run in parallel. When not set, the number of jobs is set to the number of CPUs detected.",
  },
  {
    displayName: "Batch",
    name: "batch",
    type: "string",
    default: "50000",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "The number of rows per batch to load into memory, before running in parallel. Automatically determined for CSV files with more than 50000 rows. Set to 0 to load all rows in one batch. Set to 1 to force batch optimization even for files with less than 50000 rows. [default: 50000]",
  },
  {
    displayName: "Base Url",
    name: "baseUrl",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Base URL of the OpenAI API-compatible endpoint. Precedence: this flag > QSV_LLM_BASE_URL env var > http://localhost:1234/v1",
  },
  {
    displayName: "Model",
    name: "model",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Model name compatible with the OpenAI API spec. Precedence: this flag > QSV_LLM_MODEL env var > openai/gpt-oss-20b",
  },
  {
    displayName: "Api Key",
    name: "apiKey",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      'API key for Bearer token authentication. Precedence: this flag > QSV_LLM_APIKEY env var. Set to "NONE" to suppress sending a key. Required for non-localhost URLs.',
  },
  {
    displayName: "Max Tokens",
    name: "maxTokens",
    type: "string",
    default: "10000",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Maximum number of tokens in the LLM output. Set to 0 to not send a max_tokens limit (automatically used for localhost endpoints). [default: 10000]",
  },
  {
    displayName: "Timeout",
    name: "timeout",
    type: "string",
    default: "300",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Timeout for each LLM request in seconds (0 = no timeout). [default: 300]",
  },
  {
    displayName: "Addl Props",
    name: "addlProps",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      'Additional model properties as a JSON object, e.g. \'{"reasoning_effort": "high", "temperature": 0.2}\'',
  },
  {
    displayName: "Prompt",
    name: "prompt",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "MiniJinja prompt template rendered per record. Overrides the default prompt. Reference columns by their safe name.",
  },
  {
    displayName: "Prompt File",
    name: "promptFile",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Read the prompt template from a file. Ignored if --prompt is set.",
  },
  {
    displayName: "Rate Limit",
    name: "rateLimit",
    type: "string",
    default: "0",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Seconds to sleep between LLM requests to avoid provider rate limits. Accepts fractional seconds (e.g. 0.5). [default: 0]",
  },
  {
    displayName: "On Error",
    name: "onError",
    type: "string",
    default: "fail",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      'What to do when an LLM request fails: "fail" aborts; "skip" writes an "<ERROR: ...>" cell and continues. [default: fail]',
  },
  {
    displayName: "User Agent",
    name: "userAgent",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Custom user agent for LLM requests. Supports variables like $QSV_VERSION.",
  },
  {
    displayName: "Cache Dir",
    name: "cacheDir",
    type: "string",
    default: "~/.qsv-cache/apply-summarize",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "Directory for the disk cache. [default: ~/.qsv-cache/apply-summarize] The cache TTL defaults to 28 days; set the QSV_DISKCACHE_TTL_SECS env var to change it. A value of 0 disables time-based expiration (entries are cached indefinitely). Use --no-cache to disable caching.",
  },
  {
    displayName: "No Cache",
    name: "noCache",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "Disable the disk cache (one LLM call per row, always).",
  },
  {
    displayName: "Fresh",
    name: "fresh",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "Force fresh LLM calls, refreshing any cached values.",
  },
  {
    displayName: "Stats",
    name: "stats",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      'Append two extra columns per row alongside --new-column: "<new-column>_elapsed_ms" (inference time in milliseconds) and "<new-column>_tokens" (total tokens used). For cached rows, these report the values captured when the summary was first computed.',
  },
  {
    displayName: "No Headers",
    name: "noHeaders",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "When set, the first row will not be interpreted as headers.",
  },
  {
    displayName: "Delimiter",
    name: "delimiter",
    type: "string",
    default: "",
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description:
      "The field delimiter for reading CSV data. Must be a single character. (default: ,)",
  },
  {
    displayName: "Progressbar",
    name: "progressbar",
    type: "boolean",
    default: false,
    displayOptions: {
      show: {
        operation: ["apply"],
      },
    },
    description: "Show progress bars. Not valid for stdin.",
  },
];
